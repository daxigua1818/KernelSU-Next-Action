# ========== 一加6T / 安卓11 / 内核4.9 ==========

# 这里名字必须跟 workflow 读的 KEY 一致！
KERNEL_SOURCE=https://github.com/LineageOS/android_kernel_oneplus_sdm845
KERNEL_SOURCE_BRANCH=lineage-18.1
KERNEL_CONFIG=lineageos_sdm845_defconfig
KERNEL_IMAGE_NAME=Image.gz-dtb
ARCH=arm64

# KernelSU
ENABLE_KERNELSU=true
KERNELSU_TAG=main

# 关掉会炸的选项（4.9 不支持）
DISABLE-LTO=true
ADD_OVERLAYFS_CONFIG=true
DISABLE_CC_WERROR=true
ENABLE_CCACHE=false

# 一加6T 不需要 dtbo 单独打包（Lineage 打进 Image.gz-dtb 了）
NEED_DTBO=false
BUILD_BOOT_IMG=false
SOURCE_BOOT_IMAGE=

# 先关掉这些，跑通后再开
ENABLE_LXC=false
ENABLE_NETHUNTER=false
